node_modules/
dist/
build/
coverage/

.env
.env.local
.env.*.local
# Tracked deliberately: the assignment's local DB credentials, so the
# delivered archive/clone runs without a setup step. Not real secrets.
!server/.env

.DS_Store
.idea/
.vscode/
*.log
npm-debug.log*

tmp/
