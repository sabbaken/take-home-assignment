# All values below are the defaults baked into the app — the server starts
# without an .env file. Copy this file to .env only if you need to override them.
PORT=3000
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=password
DB_NAME=assignment_db
