# Committed on purpose: these are the assignment's own local MySQL credentials
# (the same ones baked into server/db/Dockerfile), not secrets — so the project
# runs right after unzipping. A real service would keep this file out of VCS.
PORT=3000
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=password
DB_NAME=assignment_db
