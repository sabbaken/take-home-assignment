/**
 * Mirror of the server's `departments` entity — a local copy rather than a
 * shared workspace package; see README → Trade-offs.
 */
export interface Department {
  id: number;
  name: string;
}
